name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      PG_VERSION: 11
    strategy:
      matrix:
        ruby-version: [2.4.1]

    steps:
      - uses: actions/checkout@v2
      - name: Set up Ruby ${{ matrix.ruby-version }}
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
      - name: Install Postgres pkgs
        run: sudo apt-get install postgresql-client-$PG_VERSION postgresql-server-dev-$PG_VERSION
      - name: verify postgres
        run: psql -U postgres -d postgres -c 'select setting from pg_settings where name = $m$server_version$m$;'
      - name: Install Gems
        run: bundle install
      - name: Run tests
        run: bundle exec rspec
